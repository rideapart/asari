require_relative '../../asari_spec_helper'

describe Asari::ClientManager do
  subject {Asari::ClientManager}

  it "holds a version, region, mode, keys" do

  end

  it "provides a lazy-loaded cached Asari HTTP client, AWS SDK1, and AWS SDK2 clients" do

  end

  it "choses the appropriate document, domain, and search client depending on API version" do

  end

  it "passes on any API method using either version to appropriate client" do

  end

end
