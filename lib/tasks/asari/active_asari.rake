namespace :asari_rails do
# :install
# generate examples of all the required adapters, everything that doesn't go through config,
# maybe example of config too?
  task install: :environment do |models|
  end
end
